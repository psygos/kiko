[package]
name = "kiko-slam"
version = "0.1.0"
edition = "2024"

[features]
default = []
record = ["oak-sys", "ctrlc", "chrono"]
vio = []
ort-coreml = ["ort/coreml"]
ort-cuda = ["ort/cuda"]
ort-tensorrt = ["ort/tensorrt"]

[dependencies]
bytemuck = { version = "1.24.0", features = ["derive"] }
clap = { version = "4.5.28", features = ["derive", "env"] }
crossbeam-channel = "0.5.13"
libc = "0.2.153"
ort = { version = "=2.0.0-rc.12", features = ["ndarray"] }
rerun = { version = "=0.27.2", default-features = false, features = ["sdk", "server"] }
re_arrow_util = "=0.27.3"
re_log_encoding = "=0.27.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
slotmap = "1.0"
oak-sys = { path = "../oak-sys", optional = true }
ctrlc = { version = "3.4", optional = true }
chrono = { version = "0.4", optional = true }

[[bin]]
name = "kiko-slam"
path = "src/bin/kiko_slam/main.rs"

[[bench]]
name = "pose_graph_jacobians"
harness = false

[lints]
workspace = true
