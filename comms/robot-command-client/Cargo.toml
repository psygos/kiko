[package]
name = "robot-command-client"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Fail-closed host-side Kiko robot command client"

[features]
default = []
test-support = []

[dependencies]
robot-protocol = { path = "../robot-protocol" }

[lints.clippy]
all = { level = "warn", priority = -1 }

[lints.rust]
unsafe_code = "forbid"
